import { Configuration } from 'webpack';
import { Options } from './options';
declare const _default: {
    managerWebpack: (webpackConfig?: Configuration) => Configuration;
    webpack: (webpackConfig: Configuration | undefined, options: Options) => Configuration;
};
export default _default;
